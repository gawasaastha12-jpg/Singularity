{
  "name": "relativity-engine",
  "version": "1.0.0",
  "description": "Foundational structure for a framework-agnostic physics engine",
  "main": "packages/physics/index.ts",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "dev": "npm run dev --workspace=app"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
